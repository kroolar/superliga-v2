<!DOCTYPE html>
<html>
<head>
  <title>Games - Superliga 2026</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-neutral-50 min-h-screen p-4 md:p-5">
  <div class="max-w-7xl mx-auto">
    <div class="bg-white border border-neutral-200 rounded-lg shadow-sm overflow-hidden">
      <header class="p-6 md:p-8">
        <h1 class="text-3xl md:text-4xl font-bold text-neutral-900 mb-2 tracking-tight">Superliga 2026</h1>
        <p class="text-neutral-600 mb-5">Terminarz i Wyniki</p>
        <div class="border-b border-neutral-200 -mb-6 md:-mb-8">
          <nav class="flex gap-1" aria-label="Tabs">
            <%= link_to root_path, class: 'px-4 py-2 font-medium text-sm border-b-2 border-neutral-900 text-neutral-900' do %>
              Wszystkie Mecze
            <% end %>
            <%= link_to players_path, class: 'px-4 py-2 font-medium text-sm border-b-2 border-transparent text-neutral-500 hover:text-neutral-700 hover:border-neutral-300' do %>
              Statystyki Zawodników
            <% end %>
          </nav>
        </div>
      </header>
      
      <div class="p-4 md:p-6">
        <% if @games.any? %>
          <div class="overflow-x-auto -mx-4 md:mx-0">
            <table class="w-full border-collapse">
              <thead>
                <tr class="bg-neutral-50">
                  <th class="text-left px-3 md:px-4 py-3 text-xs font-semibold text-neutral-600 uppercase tracking-wider border-b border-neutral-200">Data</th>
                  <th class="text-center px-3 md:px-4 py-3 text-xs font-semibold text-neutral-600 uppercase tracking-wider border-b border-neutral-200">Wynik</th>
                  <th class="text-left px-3 md:px-4 py-3 text-xs font-semibold text-neutral-600 uppercase tracking-wider border-b border-neutral-200">Akcje</th>
                </tr>
              </thead>
              <tbody>
                <% @games.each do |game| %>
                  <tr class="hover:bg-neutral-50 transition">
                    <td class="px-3 md:px-4 py-3 md:py-4 border-b border-neutral-100">
                      <span class="font-medium text-neutral-900 text-sm md:text-base"><%= game.date.strftime('%d.%m.%Y') %></span>
                    </td>
                    <td class="px-3 md:px-4 py-3 md:py-4 border-b border-neutral-100 text-center">
                      <% if game.played && game.teams.count == 2 %>
                        <% 
                          team_a_result = game.teams.first.result || 0
                          team_b_result = game.teams.last.result || 0
                          # Convert results to score format (e.g., -5, 5 becomes 5:0)
                          if team_a_result > team_b_result
                            score = "#{team_a_result.abs}:0"
                          elsif team_b_result > team_a_result
                            score = "0:#{team_b_result.abs}"
                          else
                            score = "0:0"
                          end
                        %>
                        <span class="font-semibold text-neutral-900 text-base md:text-lg tabular-nums"><%= score %></span>
                      <% elsif !game.played %>
                        <span class="text-neutral-400">-</span>
                      <% else %>
                        <span class="text-neutral-400">N/A</span>
                      <% end %>
                    </td>
                    <td class="px-3 md:px-4 py-3 md:py-4 border-b border-neutral-100">
                      <% if game.teams.any? %>
                        <%= link_to "Szczegóły", game_path(game), class: 'inline-flex items-center px-2 md:px-3 py-1.5 md:py-2 bg-white text-neutral-900 border border-neutral-200 rounded-md font-medium text-xs md:text-sm hover:bg-neutral-50 transition' %>
                      <% elsif !game.played %>
                        <%= link_to "Losuj", generate_teams_game_path(game), class: 'inline-flex items-center px-2 md:px-3 py-1.5 md:py-2 bg-neutral-900 text-white rounded-md font-medium text-xs md:text-sm hover:bg-neutral-800 transition' %>
                      <% end %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        <% else %>
          <div class="text-center py-12 text-neutral-400 text-sm">Brak zaplanowanych meczy</div>
        <% end %>
      </div>
    </div>
  </div>
</body>
</html>
