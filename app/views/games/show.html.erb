<!DOCTYPE html>
<html>
<head>
  <title>Game Details - Superliga 2026</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen', 'Ubuntu', sans-serif;
      background: #fafafa;
      min-height: 100vh;
      padding: 20px;
      color: #0a0a0a;
    }
    
    .container {
      max-width: 1200px;
      margin: 0 auto;
    }
    
    header {
      background: white;
      border: 1px solid #e5e5e5;
      border-radius: 8px;
      padding: 30px;
      margin-bottom: 24px;
      box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
      text-align: center;
    }
    
    h1 {
      color: #0a0a0a;
      font-size: 2.2em;
      font-weight: 700;
      margin-bottom: 10px;
      letter-spacing: -0.025em;
    }
    
    .game-date {
      color: #737373;
      font-size: 1.1em;
      margin-bottom: 16px;
      font-weight: 500;
    }
    
    .status-badge {
      display: inline-block;
      padding: 6px 14px;
      border-radius: 6px;
      font-size: 0.875rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.025em;
    }
    
    .status-played {
      background: #f5f5f5;
      color: #525252;
      border: 1px solid #e5e5e5;
    }
    
    .status-upcoming {
      background: #0a0a0a;
      color: white;
    }
    
    .back-link {
      display: inline-block;
      margin-top: 16px;
      padding: 8px 16px;
      background: white;
      color: #0a0a0a;
      text-decoration: none;
      font-weight: 500;
      border: 1px solid #e5e5e5;
      border-radius: 6px;
      font-size: 0.875rem;
      transition: background 0.2s;
    }
    
    .back-link:hover {
      background: #f5f5f5;
    }
    
    .teams-container {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
      gap: 20px;
      margin-bottom: 20px;
    }
    
    .team-card {
      background: white;
      border: 1px solid #e5e5e5;
      border-radius: 8px;
      padding: 24px;
      box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
    }
    
    .team-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 20px;
      padding-bottom: 16px;
      border-bottom: 1px solid #e5e5e5;
    }
    
    .team-title {
      font-size: 1.5em;
      color: #0a0a0a;
      font-weight: 700;
      letter-spacing: -0.025em;
    }
    
    .team-result {
      font-size: 1.75em;
      font-weight: 700;
      padding: 8px 16px;
      border-radius: 6px;
      min-width: 70px;
      text-align: center;
    }
    
    .result-positive {
      background: #f5f5f5;
      color: #0a0a0a;
      border: 2px solid #0a0a0a;
    }
    
    .result-negative {
      background: white;
      color: #525252;
      border: 2px solid #e5e5e5;
    }
    
    .result-neutral {
      background: #f5f5f5;
      color: #525252;
      border: 1px solid #e5e5e5;
    }
    
    .result-none {
      background: white;
      color: #a3a3a3;
      border: 1px solid #e5e5e5;
      font-size: 1em;
    }
    
    .players-list {
      list-style: none;
    }
    
    .player-item {
      padding: 12px 16px;
      margin-bottom: 8px;
      background: #fafafa;
      border: 1px solid #f5f5f5;
      border-radius: 6px;
      transition: all 0.15s;
    }
    
    .player-item:hover {
      background: white;
      border-color: #e5e5e5;
    }
    
    .player-name {
      font-size: 0.95em;
      font-weight: 500;
      color: #0a0a0a;
    }
    
    .no-players {
      text-align: center;
      padding: 32px;
      color: #a3a3a3;
      font-size: 0.95em;
      background: #fafafa;
      border: 1px solid #f5f5f5;
      border-radius: 6px;
    }
    
    .no-teams {
      background: white;
      border: 1px solid #e5e5e5;
      border-radius: 8px;
      padding: 48px;
      text-align: center;
      box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
    }
    
    .no-teams p {
      color: #737373;
      font-size: 1.1em;
      margin-bottom: 20px;
    }
    
    .btn-generate {
      display: inline-block;
      padding: 10px 20px;
      background: #0a0a0a;
      color: white;
      text-decoration: none;
      border-radius: 6px;
      font-weight: 500;
      font-size: 0.95em;
      transition: background 0.2s;
    }
    
    .btn-generate:hover {
      background: #262626;
    }
    
    @media (max-width: 768px) {
      .teams-container {
        grid-template-columns: 1fr;
      }
      
      h1 {
        font-size: 1.5em;
      }
      
      .team-result {
        font-size: 1.5em;
        min-width: 60px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <h1>Game Details</h1>
      <div class="game-date">
        <%= @game.date.strftime('%A, %B %d, %Y at %I:%M %p') %>
      </div>
      <span class="status-badge <%= @game.played ? 'status-played' : 'status-upcoming' %>">
        <%= @game.played ? 'Played' : 'Upcoming' %>
      </span>
      <br>
      <%= link_to '← Back to Games', root_path, class: 'back-link' %>
    </header>
    
    <% if @game.teams.any? %>
      <div class="teams-container">
        <% @game.teams.each_with_index do |team, index| %>
          <div class="team-card">
            <div class="team-header">
              <h2 class="team-title">Team <%= index == 0 ? 'A' : 'B' %></h2>
              <% if team.result %>
                <div class="team-result <%= team.result > 0 ? 'result-positive' : (team.result < 0 ? 'result-negative' : 'result-neutral') %>">
                  <%= team.result > 0 ? '+' : '' %><%= team.result %>
                </div>
              <% else %>
                <div class="team-result result-none">TBD</div>
              <% end %>
            </div>
            
            <% if team.players.any? %>
              <ul class="players-list">
                <% team.players.each do |player| %>
                  <li class="player-item">
                    <span class="player-name"><%= player.name %></span>
                  </li>
                <% end %>
              </ul>
            <% else %>
              <div class="no-players">
                No players assigned yet
              </div>
            <% end %>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="no-teams">
        <p>No teams have been created for this game yet</p>
        <%= link_to "Generate Teams", generate_teams_game_path(@game), method: :post, class: 'btn-generate' %>
      </div>
    <% end %>
  </div>
</body>
</html>
